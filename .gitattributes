# Vendor duplicate static files in api-gateway (same files exist in frontend-service)
# This makes Java the primary language while HTML/CSS/JS still show from frontend-service
api-gateway/src/main/resources/static/** linguist-vendored=true
